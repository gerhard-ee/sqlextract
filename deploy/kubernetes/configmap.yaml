apiVersion: v1
kind: ConfigMap
metadata:
  name: sqlextract-config
data:
  DB_TYPE: "mssql"
  TABLE_NAME: "your_table"
  OUTPUT_FILE: "/data/output.csv"
  BATCH_SIZE: "1000"
  LOG_LEVEL: "info"
  MAX_RETRIES: "3"
  RETRY_DELAY: "5s"
  TIMEOUT: "30m"
  CHECKPOINT_INTERVAL: "5m" 